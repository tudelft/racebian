Index: jessie-stage2/rootfs/boot/firmware/config.txt
===================================================================
--- jessie-stage2.orig/rootfs/boot/firmware/config.txt
+++ jessie-stage2/rootfs/boot/firmware/config.txt
@@ -14,5 +14,7 @@
 # Additional overlays and parameters are documented
 # /boot/firmware/overlays/README
+# disable bluetooth module to get PL011 UART mapped to GPIO 14/15
+dtoverlay=disable-bt

 # Automatically load overlays for detected cameras
 camera_auto_detect=1
